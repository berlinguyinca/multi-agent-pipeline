# Adapters

Provider integrations for Claude, Codex, Ollama, and Hermes.

- `adapter-factory.ts`: builds provider adapters from config.
- `failover-runner.ts`: applies ordered fallback execution.
- `detect.ts`: probes local/provider availability.
