# Tools

Builtin tool registry and prompt injection for agent tool use.

- `registry.ts`: maps tool declarations to implementations.
- `inject.ts`: adds the tool catalog to prompts.
- `builtin/`: concrete tool implementations.
