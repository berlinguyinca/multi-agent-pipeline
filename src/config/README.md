# Config

Pipeline configuration defaults, schema validation, and loading.

- `defaults.ts`: default runtime behavior.
- `loader.ts`: merges user config with defaults.
- `schema.ts`: validates config shape and durations.
